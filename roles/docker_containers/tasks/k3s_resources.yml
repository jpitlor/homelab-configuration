---
- name: Create homelab namespace
  run_once: true
  kubernetes.core.k8s:
    name: '{{ kubernetes_homelab_namespace }}'
    api_version: v1
    kind: Namespace
    state: present

- name: Clone SnapshotController repo
  become: true
  run_once: true
  ansible.builtin.git:
    force: true
    repo: 'https://github.com/kubernetes-csi/external-snapshotter'
    dest: '{{ homelab_data_folder }}/external-snapshotter'
    version: 'v8.5.0'

- name: Get CRD file list
  run_once: true
  ansible.builtin.find:
    paths: 
      - "{{ homelab_data_folder }}/external-snapshotter/client/config/crd/"
      - "{{ homelab_data_folder }}/external-snapshotter/deploy/kubernetes/snapshot-controller/*.yaml"
    patterns: "*.yaml"
  register: crd_definitions

- name: Update namespaces
  become: true
  run_once: true
  ansible.builtin.lineinfile:
    dest: '{{ item.path }}'
    regexp: '([ ]+)namespace: .*'
    line: '\1namespace: homelab'
    state: present
    backrefs: true
  with_items: "{{ crd_definitions.files }}"

- name: Install SnapshotController CRDs
  run_once: true
  kubernetes.core.k8s:
    state: present
    src: '{{ item.path }}'
  with_items: "{{ crd_definitions.files }}"

- name: Install CRD dependencies
  run_once: true
  kubernetes.core.k8s:
    state: present
    definition:
      - apiVersion: helm.cattle.io/v1
        kind: HelmChart
        metadata:
          name: cloudnative-pg-chart
          namespace: '{{ kubernetes_homelab_namespace }}'
        spec:
          repo: https://cloudnative-pg.github.io/charts
          chart: cloudnative-pg
          version: 0.26.0
          targetNamespace: '{{ kubernetes_homelab_namespace }}'
      - apiVersion: helm.cattle.io/v1
        kind: HelmChart
        metadata:
          name: prometheus-operator-crds-chart
          namespace: '{{ kubernetes_homelab_namespace }}'
        spec:
          repo: https://prometheus-community.github.io/helm-charts
          chart: prometheus-operator-crds
          version: 17.0.2
          targetNamespace: '{{ kubernetes_homelab_namespace }}'
      - apiVersion: helm.cattle.io/v1
        kind: HelmChart
        metadata:
          name: csi-driver-smb-chart
          namespace: '{{ kubernetes_homelab_namespace }}'
        spec:
          repo: https://raw.githubusercontent.com/kubernetes-csi/csi-driver-smb/master/charts
          chart: csi-driver-smb
          version: v1.16.0
          targetNamespace: '{{ kubernetes_homelab_namespace }}'
      - apiVersion: helm.cattle.io/v1
        kind: HelmChart
        metadata:
          name: cert-manager-chart
          namespace: '{{ kubernetes_homelab_namespace }}'
        spec:
          chart: oci://quay.io/jetstack/charts/cert-manager
          version: v1.18.2
          targetNamespace: '{{ kubernetes_homelab_namespace }}'
          valuesContent: |-
            crds:
              enabled: true
      - apiVersion: helm.cattle.io/v1
        kind: HelmChart
        metadata:
          name: velero
          namespace: '{{ kubernetes_homelab_namespace }}'
        spec:
          chart: velero
          repo: https://vmware-tanzu.github.io/helm-charts
          version: 12.0.0
          targetNamespace: '{{ kubernetes_homelab_namespace }}'
          valuesContent: |-
            credentials:
              secretContents:
                cloud: {{ lookup('ansible.builtin.env', 'GOOGLE_SERVICE_ACCOUNT_JSON') | quote }}
            snapshotsEnabled: true
            metrics:
              enabled: true
              scrapeInterval: 30s
            configuration:
              backupStorageLocation:
                - name: "gcs-backup-location"
                  provider: "gcp"
                  bucket: "dev-pitlor-homelab-container-backups"
              volumeSnapshotLocation:
                - name: "gcs-snapshot-location"
                  provider: "gcp"
              defaultBackupStorageLocation: "gcs-backup-location"
              defaultVolumeSnapshotLocations: 
                gcp: "gcs-snapshot-location"
            initContainers:
              - name: velero-plugin-for-gcp
                image: velero/velero-plugin-for-gcp:v1.8.0
                volumeMounts:
                  - mountPath: /target
                    name: plugins
      - apiVersion: helm.cattle.io/v1
        kind: HelmChart
        metadata:
          name: feature-discovery-chart
          namespace: '{{ kubernetes_homelab_namespace }}'
        spec:
          repo: https://kubernetes-sigs.github.io/node-feature-discovery/charts
          chart: node-feature-discovery
          version: v0.18.3
          targetNamespace: '{{ kubernetes_homelab_namespace }}'
      - apiVersion: helm.cattle.io/v1
        kind: HelmChart
        metadata:
          name: nvidia-k8s-device-plugin-chart
          namespace: '{{ kubernetes_homelab_namespace }}'
        spec:
          repo: https://nvidia.github.io/k8s-device-plugin
          chart: nvidia-device-plugin
          version: v0.18.2
          targetNamespace: '{{ kubernetes_homelab_namespace }}'
          valuesContent: |-
            runtimeClassName: nvidia
            gfd:
              enabled: true
      - apiVersion: helm.cattle.io/v1
        kind: HelmChart
        metadata:
          name: longhorn-chart
          namespace: '{{ kubernetes_homelab_namespace }}'
        spec:
          repo: https://charts.longhorn.io
          chart: longhorn
          version: v1.11.1
          targetNamespace: '{{ kubernetes_homelab_namespace }}'
          valuesContent: |-
            namespaceOverride: '{{ kubernetes_homelab_namespace }}'
            service:
              ui:
                type: NodePort
            defaultSettings:
              createDefaultDiskLabeledNodes: true
              storageReservedPercentageForDefaultDisk: 15
              storageMinimalAvailablePercentage: 15
              orphanResourceAutoDeletion: ""
              orphanResourceAutoDeletionGracePeriod: 300
      - apiVersion: snapshot.storage.k8s.io/v1
        kind: VolumeSnapshotClass
        metadata:
          name: longhorn
        driver: driver.longhorn.io
        deletionPolicy: Delete

- name: Pause for Cert Manager to install
  ansible.builtin.pause:
    seconds: 90

- name: Download barman cloud plugin
  run_once: true
  ansible.builtin.get_url:
    url: https://github.com/cloudnative-pg/plugin-barman-cloud/releases/download/v0.6.0/manifest.yaml
    dest: ~/barman-cloud-plugin.yaml
    mode: '0664'

- name: Put proper namespace in barman cloud plugin
  run_once: true
  ansible.builtin.replace:
    path: ~/barman-cloud-plugin.yaml
    regexp: 'cnpg-system'
    replace: '{{ kubernetes_homelab_namespace }}'

- name: Apply barman cloud plugin manifest to the cluster.
  run_once: true
  kubernetes.core.k8s:
    state: present
    src: ~/barman-cloud-plugin.yaml

- name: Install homelab chart
  run_once: true
  tags:
    - homelab_chart
  vars:
    postgres_values:
      grafana: 
        postgres: &db_params
          backupId: "{{ homelab_chart_backup_id | default(omit) }}"
          recoverFromBackupId: "{{ homelab_chart_restore_from_backup_id | default(omit) }}"
          recoverFromBackupTime: "{{ homelab_chart_restore_from_backup_time | default(omit) }}"
      vaultwarden: 
        postgres: *db_params
      immich: 
        postgres: *db_params
      nextcloud: 
        postgres: *db_params
      gitea: 
        postgres: *db_params
      infisical-standalone: 
        postgres: *db_params
      guacamole: 
        postgres: *db_params
      mealie: 
        postgres: *db_params
      coder: 
        postgres: *db_params
      authentik: 
        postgres: *db_params
      dawarich: 
        postgres: *db_params
      open-webui: 
        postgres: *db_params
      homebox: 
        postgres: *db_params
      open-archiver: 
        postgres: *db_params
  kubernetes.core.k8s:
    state: present
    definition:
      apiVersion: helm.cattle.io/v1
      kind: HelmChart
      metadata:
        name: homelab-chart
        namespace: '{{ kubernetes_homelab_namespace }}'
      spec:
        chart: oci://us-central1-docker.pkg.dev/dev-pitlor-homelab/homelab-chart/homelab-containers
        version: '{{ homelab_chart_version }}'
        targetNamespace: '{{ kubernetes_homelab_namespace }}'
        valuesContent: |-
          google:
            serviceAccountJson: {{ lookup('ansible.builtin.env', 'GOOGLE_SERVICE_ACCOUNT_JSON') | quote }}

          nas:
            password: {{ lookup('ansible.builtin.env', 'NAS_PASSWORD') | quote }}

          blueskyPds:
            jwtSecret: {{ lookup('ansible.builtin.env', 'BLUESKY_JWT_SECRET') | quote }}
            adminPassword: {{ lookup('ansible.builtin.env', 'BLUESKY_ADMIN_PASSWORD') | quote }}
            plcRotationKey: {{ lookup('ansible.builtin.env', 'BLUESKY_PLC_ROTATION_KEY') | quote }}

          infisical:
            encryptionKey: {{ lookup('ansible.builtin.env', 'INFISICAL_ENCRYPTION_KEY') | quote }}
            authSecret: {{ lookup('ansible.builtin.env', 'INFISICAL_AUTH_SECRET') | quote }}   

          postfix:
            username: {{ lookup('ansible.builtin.env', 'POSTFIX_USERNAME') | quote }}  
            password: {{ lookup('ansible.builtin.env', 'POSTFIX_PASSWORD') | quote }}  

          openArchiver:
            meiliMasterKey: {{ lookup('ansible.builtin.env', 'OPEN_ARCHIVER_MEILI_MASTER_KEY') | quote }}  
            jwtSecret: {{ lookup('ansible.builtin.env', 'OPEN_ARCHIVER_JWT_SECRET') | quote }}  
            encryptionKey: {{ lookup('ansible.builtin.env', 'OPEN_ARCHIVER_ENCRYPTION_KEY') | quote }}  

          sunshine:
            password: {{ lookup('ansible.builtin.env', 'SUNSHINE_PASSWORD') | quote }}

          applications: 
            {{ postgres_values | to_nice_yaml(indent=2) | indent(2) }}
